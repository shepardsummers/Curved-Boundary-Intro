function [xp1, yp1, xp2, yp2] = find_the_wall_point(x1, y1, x2, y2, R, center_x, center_y)
    
    m = (y2-y1)/(x2-x1);

    b = (y1) - m*(x1);

    A = m^2-1;
    B = 2(m*b - center_x - m*center_b);
    C = center_x^2 + center_y^2 - 2*center_y*b - R^2;    

    xp1 = (-B + sqrt(B^2 - 4*A));
    yp1 = sqrt(R^2-xp1^2);

    xp2 = ;
    yp2 = sqrt(R^2-xp1^2);

end

